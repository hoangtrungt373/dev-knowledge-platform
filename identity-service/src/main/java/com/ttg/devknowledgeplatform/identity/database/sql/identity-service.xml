<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd"
                   logicalFilePath="IdentityService">

    <!-- This service's own changelog tree — separate from gateway's, following the
         extraction: identity-service migrates its own database now, nobody else's
         changelog runs against the `identity` schema and vice versa. -->
    <includeAll path="2026/0.0.1" errorIfMissingOrEmpty="false" relativeToChangelogFile="true"/>
</databaseChangeLog>
